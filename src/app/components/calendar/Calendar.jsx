import React from 'react';
import cs from 'classnames';
import Box from '../box/Box';
import s from './calendar.scss';

const Calendar = props => (
  <Box className={cs(s.calendarContainer, s.flex1)} > asd</Box>
);

export default Calendar;
